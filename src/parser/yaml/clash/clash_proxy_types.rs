//! Clash input types.
//!
//! The per-protocol schema lives in [`crate::models::clash`]; this module
//! keeps the historical path alive for the parser side.

pub use crate::models::clash::ClashProxy as ClashProxyYamlInput;
